{
	"manifest_version": 2,
	"name": "SBB UTILITY - API Key Interceptor",
	"version": "1.0",
	"description": "Intercepts the Bearer token from sbb and sends it to a specified server.",
	"permissions": ["webRequest", "storage", "<all_urls>"],
	"browser_action": {
		"default_popup": "popup.html",
		"default_title": "Settings"
	},
	"background": {
		"scripts": ["background.js"]
	}
}
